apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: oneingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: 
    http:
      paths:
       -  path: /
            pathType: Prefix
            backend:
              service: 
                name: todoapp
                port:
                  number: 80
         -  path: /student
            pathType: Prefix
            backend:
              service: 
                name: studentapp
                port:
                  number: 8080
        